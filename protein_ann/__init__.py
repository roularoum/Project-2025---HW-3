"""Assignment 3: Protein remote-homolog search (ESM-2 + ANN + BLAST).

This package contains reusable modules for:
- FASTA I/O
- ESM-2 embedding generation (mean pooling, last layer)
- ANN indexes (LSH, Hypercube, IVF-Flat, IVFPQ, Neural LSH)
- BLAST orchestration/parsing
- Metrics (Recall@N, QPS)
"""

